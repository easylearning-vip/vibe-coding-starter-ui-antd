apiVersion: v1
kind: Service
metadata:
  name: vibe-ui-service
  namespace: vibe-dev
  labels:
    app: vibe-ui
    environment: development
spec:
  selector:
    app: vibe-ui
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
